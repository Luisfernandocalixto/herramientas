---
import tools from "../data/tools.json";
import ToolCard from "./ToolCard.astro";
import ContentCards from "./ContenCards.astro";
const allTools = tools;
---

  <ContentCards>
    {allTools.map((tool) => <ToolCard {...tool} />)}
  </ContentCards>
  
